
[project]
name = "prop-logic-kernel"
dynamic = ["version"]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.3",
]

[tool.hatch.version]
path = "prop_logic_kernel/__init__.py"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
